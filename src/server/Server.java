package server;

/**
 * Legacy placeholder kept to avoid breaking old references to server.Server.
 *
 * Active endpoint implementations now live under the server.websocket package.
 */
@Deprecated
public final class Server {

    private Server() {
    }
}
